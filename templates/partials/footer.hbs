    <footer>
      <div class="container">
        <h3>Share this page</h3>

        <div class="social-media">
          <a href="javascript: sharePopup('facebook', popUpConfig);">
            <i class="Icon fa fa-facebook fa-fw"></i>
          </a>
          <a href="javascript: sharePopup('twitter', popUpConfig);">
            <i class="Icon fa fa-twitter fa-fw"></i>
          </a>
        </div><!-- .social-media -->

        <nav class="nav-footer">
          <a href="http://help-au.everydayhero.com/">HELP</a> |
          <a href="https://everydayhero.com/au/terms/privacy">PRIVACY</a> |
          <a href="https://everydayhero.com/au/terms">TERMS &amp; CONDITIONS</a>
        </nav><!-- .nav-footer -->

        <div class="powered-by">
          powered by <a href="http://everydayhero.com/"><img src="img/logo-edh.png" alt="everydayhero" title="everydayhero" /></a>
        </div><!-- .powered-by -->
      </div><!-- .container -->
    </footer>
  </div><!-- .main-wrapper -->

  <script src="//code.jquery.com/jquery-2.1.3.min.js"></script>
  <script>
    // leaderboard
    edh.widgets.renderWidget('leaderboard', 'Leaderboard', {
      campaignUids: ['NZ-932'],
      limit: 5,
      pageSize: 5,
      backgroundColor: "transparent",
      i18n: {
        heading: "Top Fundraisers",
      }
    });


    // Sharing
    var topWindow = '';
    var popUpConfig = {
      toolbar: 0,
      status: 0,
      width: 640,
      height: 320
    }
    var shareURLs = {
      'facebook': "http://www.facebook.com/sharer.php?u={url}&title={title}",
      'twitter': "https://twitter.com/share?url={url}&text={title}",
    }
    var shareURL = "http://www.amyschristmas.org.nz";
    var shareTitle = "Help light up Amy’s Christmas Tree";

    function sharePopup(type, config) {
      url = shareURLs[type];
      url = url.replace('{url}', shareURL);
      url = url.replace('{title}', shareTitle);

      if (topWindow === '') {
        var windowTop  = window.screenTop ? window.screenTop : window.screenY;
        var windowLeft = window.screenLeft ? window.screenLeft : window.screenX;

        var windowHeight = window.innerHeight;
        var windowWidth = window.innerWidth;
      } else {
        var windowTop = topWindow['windowTop'];
        var windowLeft = topWindow['windowLeft'];

        var windowHeight = topWindow['windowHeight'];
        var windowWidth = topWindow['windowWidth'];
      }

      config.top  = windowTop + ((windowHeight / 2) - (config.height / 2));
      config.left = windowLeft + ((windowWidth / 2) - (config.width / 2));

      config = JSON.stringify(config);
      config = config.replace('{', '');
      config = config.replace('}', '');
      config = config.replace(/"/gi, '');
      config = config.replace(/:/gi, '=');

      var windowRef = window.open(url, 'shareWindow', config);
    }
  </script>
</body>
</html>
